{
  "name": "accordion-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently 'cd accordion-app && npm i && ng serve --open' 'cd accordion-server && npm i && npm run dev' "
  },
  "keywords": [],
  "author": "Prakhyat",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.4.0"
  }
}